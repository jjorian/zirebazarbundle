class MediaView {
    constructor(
        ID,
        Status,
        KeyId,
        KeyType,
        URL,
        Type,
        Priorirty,
        Title) {
        this.ID = ID;
        this.Status = Status;
        this.KeyId = KeyId;
        this.KeyType = KeyType;
        this.URL = URL;
        this.Type = Type;
        this.Priorirty = Priorirty;
        this.Title = Title;
    }
}

export default MediaView;
